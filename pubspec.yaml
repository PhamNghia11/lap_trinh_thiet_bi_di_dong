name: flix_app
description: "FLIX - Ứng dụng Tra cứu Phim (Movie Lookup App)"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  carousel_slider: ^5.0.0
  http: ^1.2.2
  flutter_secure_storage: ^9.2.4
  url_launcher: ^6.3.1
  shared_preferences: ^2.3.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
